import React from "react";

// this is going to be a functional component because we are not using state 
// we can pass "props" to the component, such as toggleClick function

const Toggle = (props) => {
    return (
        <div>This will be toggle</div>
    )
}


export default Toggle;